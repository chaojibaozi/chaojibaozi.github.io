# 2026-07-26 工作日志

## 全量重跑路径 bug 修复（重要坑）
- 现象：上一轮后台全量重跑（task Jorzop）启动后瞬间崩溃，日志报
  `ENOENT ...\Keyword-Segmentation-Tool\build\build\_feed02_keywords.json`。
- 根因：`build/_harness02full.cjs` 与 `build/_gen_deliverables02.cjs` 内部使用**相对路径**
  `build/_feed02_keywords.json`、`build/_feed02_full_classified.json`（相对于 cwd）。
  上一轮误用 `cd build/` 再运行，相对路径被拼成 `build/build/...`。
- 修复：必须从**项目根目录** `D:\BianCheng\Keyword-Segmentation-Tool` 运行：
  `node build/_harness02full.cjs && node build/_gen_deliverables02.cjs`。
  （生成器内 `require('./sheetjs.full.min.js')` 基于 __dirname，不受 cwd 影响，正常。）
- 教训：**凡涉及本仓库 harness/生成器脚本，一律从根目录启动**，不要 cd 进 build。

## 当前全量重跑状态
- 后台任务 **0phAc0**（20:3x 启动，约80分钟）：用最新词典 `index.html`（19:31 版，含持续测试15处扩充：
  京东/淘宝/天猫/拼多多等 ecommerce、RPA/it-services、耳朵/鼻子/咽喉/中耳/阴道/白带/结石/打呼噜等 medical、
  纯净水/苏打水/红牛/生鲜/洗发水/牙膏等 food-beverage、耳机/手机/相机/电视/主板/pcb/airpods 等 electronics、
  电池/水饮/粮油/洗化/纸品等 daily-necessities、风投 finance）重跑全部 897,709 词。
- 已完成 discoverDomainVocab + buildRegexCache，进入主分类循环。
- 完成后自动刷新 `D:\2019年10月任务\投喂物料-02_分类结果.xlsx` 与 `分词逻辑优化报告-02.html`。
- 旧产物（15:56 json / 16:57 xlsx）基于旧词典，待本次覆盖。

## 用户要求"每次任务必须核对真实状态"（已落实为长期习惯）
- 用户原话："现在是真实在跑吗，你每次做任务要核对状态"。
- 核对 0phAc0 真实状态（20:36 抽样）：
  - 后台任务 Status=running，Duration 4m42s 且在增长；
  - PID 1003 cmdline = `node.exe build/_harness02full.cjs`，State=R，PPid=1002(shell)；
  - **CPU 间隔采样铁证**：utime+stime 8 秒内 281830→290217（+8387 ticks），证明真在消耗 CPU 干活；
  - VmRSS≈501MB（out 数组持续 push，正常）。
  - **结论：0phAc0 确实真实在跑，之前"日志停在5万词"是 Node console.log 写文件块缓冲未 flush 的假象，非进程卡死。**
- 已将该核对方法 + 路径坑 + 缓冲坑写入 `~/.workbuddy/MEMORY.md`（跨项目长期习惯）。
- 预计完成：按实测 ~2.36ms/词，897709 词 ≈ 35 分钟（20:33 启动 → ~21:08），完成后系统通知并核对产物时间戳是否从 15:56 更新。

## 20:51 真实状态核对（落实"必须告真实状态"硬规则）
- 0phAc0 真实在跑：PID 1003 State=R，CPU 8秒 +8266 ticks，日志已 flush 显示 `progress 450000/897709 @1047s`（约50%）。
- 20:36 当时"卡在5万词"确为 Node 日志块缓冲假象（彼时实际已 20 万 @492s），现已实时可见。
- 用户升级为硬性约束："以后要注意告诉我任务运行和进程的真实状态，否则任务失败" → 已写入 `~/.workbuddy/MEMORY.md`（跨项目长期规则 + 核对清单 6 项 + 已知坑）。
- 预计 ~21:08 完成，届时核产物时间戳（json 应从 15:56 更新）再汇报。

## 20:55 进度核对
- 0phAc0 harness PID 1003 仍在跑（cmdline=`node.exe build/_harness02full.cjs`）。
- 日志实时进度 `progress 550000 / 897709 @ 1281s`（约 **61%**）；对比 20:51 的 45 万，4 分钟稳涨 10 万，速度 ~2.36ms/词，缓冲已 flush 可信。
- 剩余约 13.6 分钟，预计 ~21:08 完成；产物仍旧时间戳（harness 未完，符合预期）。
- 注：本次核对脚本误把自身 bash 进程(3306)当匹配项（cmdline 含 eval 字符串 `_harness02full`），已排除；以 PID 1003 明确 cmdline + 日志实时进度为准。

## 概念澄清（用户问）
- **最新词典** = `index.html` 内的分类规则+词库（输入侧/引擎），决定"怎么分"。
- **全量版** = 897,709 词跑完的分类结果（输出侧/产物：json+xlsx），决定"90万词各归哪"。
- 关系：改了词典必须重跑全量，结果才反映新词。

## 21:20 真实完成核验（0phAc0 已结束）
- **进程**：harness/generator 进程已退出，无残留（只剩本次核对命令自身 bash）。
- **产物时间戳全部更新为新时间**：
  - `build/_feed02_full_classified.json` = 21:14:02（223,699,023 B）
  - `D:\2019年10月任务\投喂物料-02_分类结果.xlsx` = 21:16:32（418,309,067 B）
  - `D:\2019年10月任务\分词逻辑优化报告-02.html` = 21:16:32
- **日志自洽**：`DONE n=897709 crashes=0 total=41.6min` → `XLSX written ... 897710 rows` → `ALL DONE 2026-07-26_21:16:57`，Exit Code 0 / Signal none。
- **内容级抽查（确认新词典真的生效，非旧版）**：
  - JSON 数组长度 = 897709（与日志一致）；
  - 京东 => brand/ecommerce；纯净水 => core/food-beverage；中耳炎 => core/medical —— 三项均按 19:31 最新词典正确归类；
  - 结论：**全量版已基于最新词典刷新成功，本次为真实完成**（对比 Jorzop 的假完成，证据链完整）。
- 实际耗时 41.6 分钟（20:33→21:16），略长于预估 35 分钟（flush/生成开销）。
